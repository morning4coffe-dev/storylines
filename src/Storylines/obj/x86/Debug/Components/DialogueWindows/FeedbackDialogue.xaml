<ContentDialog
    x:Class="Storylines.Components.DialogueWindows.FeedbackDialogue"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" CornerRadius="8"><!--Closed="ContentDialog_Closed"-->

    <!--<StackPanel Spacing="8" VerticalAlignment="Stretch" HorizontalAlignment="Left" Width="300" Margin="4" Orientation="Vertical">
        <TextBlock x:Uid="exportText" Style="{StaticResource DialogueWindowHeaderStyle}"/>

        <StackPanel Spacing="8" Orientation="Horizontal" HorizontalAlignment="Center" Visibility="Visible" Margin="0, 8, 0, 0">
            <StackPanel.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition x:Name="chooseWhatToExportAnimation" IsStaggeringEnabled="True" FromVerticalOffset="30"/>
                </TransitionCollection>
            </StackPanel.ChildrenTransitions>

            <ToggleButton x:Name="chooseExportChaptersButton" Background="Transparent" Width="146" Height="78" CornerRadius="4" Click="OnSuggestionButton_Click">
                <ToggleButton.Content>
                    <StackPanel Spacing="10">
                        <FontIcon Glyph=""/>
                        <TextBlock x:Uid="chaptersExport"/>
                    </StackPanel>
                </ToggleButton.Content>
            </ToggleButton>

            <ToggleButton x:Name="chooseExportDialoguesButton" Background="Transparent" Width="146" Height="78" CornerRadius="4" Click="OnProblemButton_Click">
                <ToggleButton.Content>
                    <StackPanel Spacing="10">
                        <FontIcon Glyph=""/>
                        <TextBlock x:Uid="dialoguesExport"/>
                    </StackPanel>
                </ToggleButton.Content>
            </ToggleButton>
        </StackPanel>
        
        <Grid x:Name="chooseExportChaptersPanel" Visibility="Visible">
            <StackPanel Spacing="12">
                --><!--For animation to work properly--><!--
                <StackPanel Spacing="5">
                    <TextBlock x:Uid="fileName"/>
                    <TextBox x:Name="feedbackText" Width="300" Height="38" Style="{StaticResource SmallTextBoxStyle}" TextChanged="OnNameTextBox_TextChanged"/>
                </StackPanel>

                <StackPanel Spacing="5">
                    <TextBlock x:Uid="fileName"/>
                    <TextBox x:Name="feedbackLongText" Width="300" Height="100" TextWrapping="Wrap" VerticalContentAlignment="Center" TextChanged="OnNameTextBox_TextChanged"/>
                </StackPanel>

                <CheckBox x:Name="analyticsCheckBox" x:Uid="includeChapterName" IsChecked="True"/>
                
                <StackPanel Margin="0, 8, 0, 0" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                    <Button x:Name="submitButton" x:Uid="exportContent" CornerRadius="4" Style="{StaticResource AccentButtonStyle}" IsEnabled="True" Width="100" Height="38" Background="{ThemeResource SystemAccentColorDark1}" Click="OnExportButton_Click"/>
                    <Button x:Uid="cancel" Width="100" Height="38" Click="OnCancelButton_Click"/>
                </StackPanel>
            </StackPanel>
            <animations:Implicit.ShowAnimations>
                <animations:TranslationAnimation Duration="0:0:0.65" From="0, -10, 0" To="0"/>
                <animations:OpacityAnimation Duration="0:0:0.65" From="0" To="1.0"/>
            </animations:Implicit.ShowAnimations>
        </Grid>
    </StackPanel>-->
</ContentDialog>

