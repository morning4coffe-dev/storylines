<UserControl x:Class="Storylines.Views.Controls.SettingsPageLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:constants="using:Storylines.Constants"
             mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel x:Name="RootPanel"
                    Margin="70,60"
                    Spacing="10"
                    HorizontalAlignment="Stretch"
                    MaxWidth="1600">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="CompactLayout" />
                    <VisualState x:Name="WideLayout">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="{x:Bind constants:LayoutConstants.SettingsWideLayoutBreakpoint}" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="RootPanel.Margin" Value="32,24,32,24" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <ContentPresenter Content="{x:Bind PageContent, Mode=OneWay}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
